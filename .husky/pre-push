. "$(dirname -- "$0")/_/husky.sh"

current_branch="$(git rev-parse --abbrev-ref HEAD)"
remote_branch="$(git remote show origin | grep "pushes to" | awk '{print $5}')"

if [ "$current_branch" = "dev" ] && [ "$remote_branch" != "main" ]; then
  echo "Only 'dev' branch can push to 'main'!"
  exit 1
fi